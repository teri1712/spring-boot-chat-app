name: CD - Staging

on:
    push:
        branches: [ develop ]

jobs:
    deploy:
        uses: ./.github/workflows/cd.yml
        with:
            GKE_CLUSTER: ${{ secrets.GKE_STAGING_CLUSTER }}
        secrets: inherit
