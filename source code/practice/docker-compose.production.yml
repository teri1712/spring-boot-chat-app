services:
  backend1:
    image: teri1712/chat-app:${IMAGE_TAG:-latest}
    environment: &backend-env
      FRONTEND_HOST_ADDRESS: http://${SERVER_IP}:4200
      AWS_S3_ENDPOINT: http://${SERVER_IP}:4566
      SPRING_PROFILES_ACTIVE: redis-cache, prod
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
      SPRING_DATASOURCE_HIKARI_MAXIMUM_POOL_SIZE: 10
      SPRING_DATASOURCE_HIKARI_MINIMUM_IDLE: 5
      ACTUATOR_USER_NAME: ${ADMIN_USER}
      ACTUATOR_USER_PASSWORD: ${ADMIN_PASSWORD}
      ACTUATOR_USER_ROLES: OPS
      SPRING_DATA_REDIS_HOST: redis
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/chatapp
      SPRING_ELASTICSEARCH_URIS: http://elasticsearch:9200
      SPRING_DATA_ELASTICSEARCH_URIS: http://elasticsearch:9200
      SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      es-init:
        condition: service_completed_successfully
    volumes:
      - ./logs:/logs
    networks:
      - backend
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:8081/actuator/health/readiness -u $$ACTUATOR_USER_NAME:$$ACTUATOR_USER_PASSWORD || exit 1" ]
      start_period: 30s
      interval: 10s
      timeout: 30s
      retries: 20
  backend2:
    image: teri1712/chat-app:${IMAGE_TAG:-latest}
    environment: *backend-env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      es-init:
        condition: service_completed_successfully
    volumes:
      - ./logs:/logs
    networks:
      - backend
    healthcheck:
      test: [ "CMD-SHELL", "curl -f http://localhost:8081/actuator/health/readiness -u $$ACTUATOR_USER_NAME:$$ACTUATOR_USER_PASSWORD || exit 1" ]
      interval: 10s
      start_period: 30s
      timeout: 30s
      retries: 20


  es-init:
    image: curlimages/curl
    depends_on:
      elasticsearch:
        condition: service_healthy
    volumes:
      - ./es:/es
    entrypoint: [ "sh", "-c", "cd /es && sh ./init.sh" ]
    networks:
      - backend

  server:
    image: nginx:latest
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      backend1:
        condition: service_healthy
      backend2:
        condition: service_healthy
    networks:
      - backend
  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    cpus: 0.5
    cpu_count: 3
    mem_limit: 500m
    networks:
      - backend

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
    volumes:
      - grafana-data:/var/lib/grafana
    depends_on:
      - prometheus
    cpu_count: 3
    cpus: 0.3
    mem_limit: 1g
    networks:
      - backend

networks:
  backend:
    external: true

volumes:
  grafana-data:
  prometheus-data: