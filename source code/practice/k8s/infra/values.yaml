postgresql:
  auth:
    database: chatapp
    username: root
    password: ""
  primary:
    persistence:
      enabled: true

redis:
  auth:
    enabled: true
    password: ""
  master:
    persistence:
      enabled: true

kafka:
  image:
    repository: bitnamilegacy/kafka
  auth:
    clientProtocol: sasl_plaintext
  persistence:
    enabled: true
