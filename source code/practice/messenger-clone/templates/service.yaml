apiVersion: v1
kind: Service
metadata:
  name: {{ include "messenger-clone.fullname" . }}
  labels:
    {{- include "messenger-clone.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "messenger-clone.selectorLabels" . | nindent 4 }}
