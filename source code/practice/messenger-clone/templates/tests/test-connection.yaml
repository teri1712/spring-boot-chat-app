apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "messenger-clone.fullname" . }}-test-connection"
  labels:
    {{- include "messenger-clone.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "messenger-clone.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
