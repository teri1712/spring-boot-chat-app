server:
  cache:
    events: false

logging:
  level:
    root: DEBUG
    com.decade.practice: TRACE
    org.springframework: DEBUG
    org:
      springframework:
        security:
          web:
            authentication:
              www: TRACE


outbox:
  scheduling:
    enabled: false