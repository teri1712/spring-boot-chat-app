ingress:
  annotations:
    kubernetes.io/ingress.allow-http: "true"
  managedCertificate:
    enabled: false
  hosts:
    - host: "t1ichat-staging.ddnsfree.com"
      paths:
        - path: /
          pathType: Prefix
